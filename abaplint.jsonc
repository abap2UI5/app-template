{
  "global": {
    "files": "/src/**/*.*"
  },
  "dependencies": [
    {
      // the abap2UI5 framework this app runs on - cloned by abaplint for
      // dependency resolution (z2ui5_if_app, z2ui5_if_client, z2ui5_cl_ui5_view_builder)
      "url": "https://github.com/abap2UI5/abap2UI5",
      // NOT pinned, and that is currently forced rather than chosen: the
      // newest release tag (1.142.0, 2026-07-21) does not contain
      // z2ui5_cl_ui5_view_builder or client->get_event( ) - both reached main
      // on 2026-08-12 - so pinning to it fails the very starter class this
      // template ships. abapGit pulls the default branch anyway, which is
      // what a reader following the quickstart installs.
      //
      // Pin it once a release carries the builder, with:
      //     "branch": "<tag>",
      // abaplint hands that to git clone --branch, which takes a tag as
      // happily as a branch. There is no "tag" key - it would be ignored in
      // silence, which looks exactly like a pin that works.
      "files": "/src/**/*.*"
    }
  ],
  "syntax": {
    "version": "v750",
    // own namespace fully checked; unknown SAP-standard objects are void so
    // the lint does not depend on a full standard-API mirror
    "errorNamespace": "^(Z|Y)"
  },
  "rules": {
    "begin_end_names": true,
    "check_ddic": true,
    "check_include": true,
    "check_syntax": true,
    "global_class": true,
    "implement_methods": true,
    "method_implemented_twice": true,
    "parser_error": true,
    "superclass_final": true,
    "unknown_types": true,
    "allowed_object_types": {
      "allowed": ["CLAS", "DEVC", "INTF", "TABL"]
    },
    "obsolete_statement": true,
    // the CLSNAME in the .clas.xml against the file name - the mistake that
    // happens when you copy the starter class and rename half of it
    "xml_consistency": true,
    // the ABAP editor strips a trailing blank when it saves, so a line that
    // ends in one comes back changed on the next pull - for everyone
    "whitespace_end": true,
    "definitions_top": false,
    "7bit_ascii": true,
    "line_length": {
      "length": 255
    },
    "object_naming": {
      "patternKind": "required",
      "clas": "^ZCL_|^ZCX_",
      "intf": "^ZIF_"
    }
  }
}
